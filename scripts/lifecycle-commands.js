const LIFECYCLE_COMMANDS = {
  dev: 'npm run osb extension-manager theme dev',// chockidar watcher that runs vite build on change
  build: 'npm run osb extension-manager theme build'// vite build
}

export {
  LIFECYCLE_COMMANDS
}